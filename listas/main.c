#include <stdio.h>
#include <stdlib.h>

int main()
{
    /// Declaro Lista
    // nodo* lista

    /// Inicializo lista
    // lista = inicLista();

    /// Creo la lista y devuelvo el inicio
    // lista = cargarLista(lista);

    /// Muestro la Lista
    // recorrerYmostrar(lista);
}
